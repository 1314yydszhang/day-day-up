//有n个人围成一圈，顺序排号。从第一个开始报数（从1到3报数），
//凡报到3的人退出圈子，问最后留下的那位是原来的几号。
//用到数组指针
int main()
{
	int n = 0;
	scanf("%d", &n);
	int num[50] = { 0 };//数组
	int i = 0;
	int* p = num;//指向数组的指针
	for (i = 0; i < n; i++)
	{
		*(p+i) = i + 1;//赋值
	}
	int m = 0;
	int k = 0;
	i = 0;
	while (m < n-1)
	{
		if (*(p+i) != 0)//如果玩家还在，则报数
		{
			k++;
		}	
		if (k == 3)//报数为3时，人走，数清，淘汰一人
		{
			*(p+i) = 0;
			k = 0;
			m++;
		}
		i++;
			if (i == n)//报数一轮时，数清
			{
				i = 0;
		}
	}
	while (*p == 0)//判断没淘汰的人
	{
		p++;
	}
	printf("%d\n", *p);
	return 0;
}
